# Images

Add your profile photo here as `profile.jpg` (or update `index.html` to use a different filename).

Recommended: Square image, at least 400×400px. JPG or PNG.

If no image is present, the site falls back to your GitHub avatar.
